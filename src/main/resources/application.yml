
#开发配置:
spring:
  mvc:
    static-path-pattern: /**
  resources:
    static-locations: classpath:/META-INF/resources/,classpath:/resources/, classpath:/static/, classpath:/public/,file:/home/cy/cyh/fileServer
  datasource:
    url: jdbc:mysql://localhost:3306/book?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
    #mysql5用com.mysql.jdbc.Driver,6+ 用com.mysql.cj.jdbc.Driver
    #使用druid数据源
    type: com.alibaba.druid.pool.DruidDataSource





#公共配置与profiles选择无关 mapperLocations指的路径是src/main/resources
mybatis:
  typeAliasesPackage: com.chen.library.**.entity
  mapperLocations: classpath:com/chen/library/**/mapper/*.xml


thymeleaf:
  mode: HTML
  cache: false
  encoding: utf-8
  enabled: true
  prefix: classpath:/templates/
  suffix: .html

server:
  port: 8087

logging:
  level:
    com.chen.library.manage.mapper : debug